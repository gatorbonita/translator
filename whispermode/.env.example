# Google Cloud Credentials (only needed for translation, NOT for transcription!)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Whisper Settings
# Model size: tiny, base, small, medium (default), large-v3
# Larger = better quality but slower
WHISPER_MODEL=medium

# Device: auto (default), cpu, cuda
# auto will use GPU if available
WHISPER_DEVICE=auto

# Logging
LOG_LEVEL=INFO

# Temporary Files
TEMP_DIR=./temp
